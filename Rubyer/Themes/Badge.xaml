<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converter="clr-namespace:Rubyer.Converters"
                    xmlns:rubyer="clr-namespace:Rubyer">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary>
            <converter:BadgeOffsetConverter x:Key="BadgeOffsetConverter" />
        </ResourceDictionary>
        <ResourceDictionary Source="pack://application:,,,/Rubyer;component/Themes/Resources/Default.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="RubyerBadge" TargetType="{x:Type rubyer:Badge}">
        <Setter Property="BadgeBrush" Value="{StaticResource Error}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource WhiteForeground}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BadgeFontSize" Value="11" />
        <Setter Property="BadgeSize" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type rubyer:Badge}">
                    <rubyer:OverlayPanel x:Name="root">
                        <rubyer:AdornerContainer>
                            <rubyer:AdornerContainer.Offset>
                                <MultiBinding Converter="{StaticResource BadgeOffsetConverter}">
                                    <Binding ElementName="root" Path="ActualWidth" />
                                    <Binding ElementName="border" Path="ActualHeight" />
                                    <Binding ElementName="border" Path="ActualWidth" />
                                    <Binding Path="BadgeSize" RelativeSource="{RelativeSource AncestorType=rubyer:Badge}" />
                                </MultiBinding>
                            </rubyer:AdornerContainer.Offset>
                            <Border x:Name="border"
                                    MinWidth="{TemplateBinding BadgeSize}"
                                    MinHeight="{TemplateBinding BadgeSize}"
                                    Background="{TemplateBinding BadgeBrush}"
                                    CornerRadius="{Binding ActualHeight, RelativeSource={RelativeSource Self}, Converter={StaticResource HalfOfDoubleConverter}}"
                                    RenderTransformOrigin="0.5 0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform />
                                </Border.RenderTransform>
                                <rubyer:OverlayPanel Margin="5 2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="text"
                                               FontSize="{TemplateBinding BadgeFontSize}"
                                               Foreground="{TemplateBinding Foreground}"
                                               Text="{TemplateBinding Text}"
                                               Visibility="Collapsed" />
                                    <ContentPresenter x:Name="badgeContent" ContentSource="BadgeContent" />
                                </rubyer:OverlayPanel>
                            </Border>
                        </rubyer:AdornerContainer>

                        <ContentPresenter Focusable="False"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </rubyer:OverlayPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHidden" Value="True">
                            <Setter TargetName="border" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="BadgeContent" Value="{x:Null}">
                            <Setter TargetName="text" Property="Visibility" Value="Visible" />
                            <Setter TargetName="badgeContent" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Text, RelativeSource={RelativeSource Self}, Converter={StaticResource IsNullOrEmptyConverter}}" Value="True"/>
                                <Condition Binding="{Binding BadgeContent, RelativeSource={RelativeSource Self}}" Value="{x:Null}"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="text" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="badgeContent" Property="Visibility" Value="Collapsed" />
                            <Setter Property="BadgeSize" Value="10" />
                        </MultiDataTrigger>
                        <EventTrigger RoutedEvent="TextChanged">
                            <BeginStoryboard>
                                <BeginStoryboard.Storyboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                         From="0.6"
                                                         To="1"
                                                         Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <BackEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="border"
                                                         Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                         From="0.6"
                                                         To="1"
                                                         Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <BackEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard.Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>